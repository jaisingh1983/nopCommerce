@model Nop.Web.Models.Books.BooksModel

@{
    Layout = "_ColumnsTwo";

    // Title
    Html.AddTitleParts(T("Web.Books.Create").Text);
    Html.AppendPageCssClassParts("html-book-create-page");
    // Page class
    Html.AppendPageCssClassParts("html-address-edit-page");
}


<form asp-route="CreateBook" method="post">
    <div class="page account-page book-create-page">
        <div class="page-title">
            <h1>@T("Web.Books.Create")</h1>
        </div>
        <div class="page-body">
            <div asp-validation-summary="ModelOnly" class="message-error"></div>
            <div class="form-group">
                @Html.LabelFor(model => model.Id, new { @class = "control-label" })
                @Html.EditorFor(model => model.Id, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Id, "", new { @class = "text-danger" })
            </div>
            <div class="form-group">
                @Html.LabelFor(model => model.Name, new { @class = "control-label" })
                @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
            </div>
            <div class="buttons">
                <input type="submit" class="button-1 save-book-button" value="@T("Common.Save")" />
            </div>
        </div>
    </div>
</form>
